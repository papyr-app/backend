name: CI/CD

on:
  push:
    branches:
      - main
      - develop

jobs:
  lint-format-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run black
        run: |
          pip install black
          black src --check

      - name: Run pylint
        run: |
          pip install pylint
          pylint src || true

      - name: Run tests
        run: |
          pytest tests

  build-and-push:
    needs: lint-format-test
    runs-on: ubuntu-latest
    if: success()

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:latest

      - name: Trigger deployment webhook for develop
        if: github.ref == 'refs/heads/develop'
        run: |
          curl -X POST https://listener.papyr-app.xyz/webhook/test?token=${{ secrets.LISTENER_TOKEN }}

      - name: Trigger deployment webhook for main
        if: github.ref == 'refs/heads/main'
        run: |
          curl -X POST https://listener.papyr-app.xyz/webhook/prod?token=${{ secrets.LISTENER_TOKEN }}
